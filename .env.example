# Server Configuration
PORT=4000
NODE_ENV=development

# Database
# PostgreSQL connection string
# Format: postgresql://user:password@localhost:5432/database_name
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/swiip

# AI Configuration
# Provider: "openai" or "openrouter"
AI_PROVIDER=openai

# OpenAI Configuration (required if AI_PROVIDER=openai)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenRouter Configuration (required if AI_PROVIDER=openrouter)
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-your-openrouter-api-key-here

# AI Model
# Default: gpt-4o-mini (cheap and fast)
# Other options: gpt-4o, gpt-3.5-turbo, claude-3-haiku, etc.
AI_MODEL=gpt-4o-mini

# Usage Limits (for free users)
# Daily AI polish limit per user
AI_DAILY_FREE_LIMIT=10

# Admin
ADMIN_KEY=your_admin_key_here

# RevenueCat Webhook
# Get this from RevenueCat Dashboard -> Integrations -> Webhooks -> Authorization header value
REVENUECAT_WEBHOOK_SECRET=your_webhook_secret_here

# App Base URL (optional, for documentation)
APP_BASE_URL=https://your-domain.com

# CORS / Socket.IO allowed origins (comma-separated)
# Example: https://app.swiip.com,https://admin.swiip.com,exp://192.168.1.10:8081
# Leave empty in non-production to allow all origins (development convenience).
ALLOWED_ORIGINS=

# Google Play / App Store reviewer bypass (verify-code endpoint)
# All three must be set to enable. Leave empty to disable reviewer login.
# Rotate REVIEWER_CODE after each store review cycle.
REVIEWER_PHONE=
REVIEWER_EMAIL=
REVIEWER_CODE=